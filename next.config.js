/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export",
  basePath: "/Hushly2",
  images: {
    unoptimized: true,
  },
  trailingSlash: true,
};

module.exports = nextConfig;
